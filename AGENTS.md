# AGENTS.md

## Cursor Cloud specific instructions

This is a **greenfield repository** (`Quest-and-Chronicle-2026`). As of the initial setup there is no application code, no build system, no dependencies, and no services.

### Current state

- Single file: `README.md`
- No programming language, framework, or package manager has been chosen yet.
- No lint, test, or build commands exist.

### When application code is added

Once the project gains source code, update this section with:

- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to run lint, tests, and the dev server.
- Any non-obvious environment setup caveats discovered during development.
